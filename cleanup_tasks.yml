---
 - hosts: submit_nodes compute_nodes
   roles:
#   - {role: common, cleanup: "yes"}
# this was just restarting nfs...
   - {role: users, cleanup: "yes"}

# - hosts: submit_nodes
#   roles:
#     - torque_restart
